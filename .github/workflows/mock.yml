name: Mock

jobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - run: echo a
  build:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - run: echo b
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo c
  foo:
    needs: [test, build]
    runs-on: ubuntu-latest
    steps:
      - run: echo djobs:
  setup:
    runs-on: ubuntu-latest
    steps:
      - run: ./setup_server.sh
  build:
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - run: ./build_server.sh
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: ./test_server.sh

